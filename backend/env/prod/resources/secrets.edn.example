;; Production secrets configuration
;;
;; WARNING: Use production Plaid credentials here.
;; NEVER commit the unencrypted secrets.edn file!
;;
;; Copy this to secrets.edn and encrypt with:
;;   cd env/prod/resources && age -r $(cat ~/.config/finance-aggregator/key.txt | grep -v "^#" | head -1 | sed 's/.*: //') -o secrets.edn.age secrets.edn && rm secrets.edn

{;; Plaid API credentials - Production
 :plaid {:client-id "your_production_client_id"
         :secret "your_production_secret"
         :environment #plaid/environment :production}

 ;; Database encryption key for production
 :database {:encryption-key nil}}
