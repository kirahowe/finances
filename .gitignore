# Data files
backend/data/
*.csv
*.pdf

# Credentials
backend/resources/config.edn
.env

# Secrets (plaintext) - DO commit .edn.age (encrypted) and .edn.example (template)
backend/resources/secrets.edn
secrets.edn
*.edn.key

# Clojure
backend/.cpcache/
backend/.nrepl-port
backend/.lsp/
backend/.clj-kondo/

# OS
.DS_Store

# React Router
/.react-router/
/build/

# Test artifacts
backend/test-results/

# Islands (TS island toolchain + esbuild output)
islands/node_modules/
backend/resources/public/js/islands/

# Datastar runtime — pinned build artifact fetched by islands/build.mjs (not vendored)
backend/resources/public/js/datastar.js

# e2e (TypeScript browser checks — Playwright lives here now, run via Node native TS)
e2e/node_modules/

