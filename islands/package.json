{
  "name": "islands",
  "private": true,
  "type": "module",
  "description": "Vanilla TS islands (esbuild) for the Datastar server-authoritative workspace. Pure logic modules live under src/lib with their vitest tests; bundles are written to backend/resources/public/js/islands.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@zag-js/combobox": "^1.41.2",
    "@zag-js/vanilla": "^1.41.2"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.9.2",
    "vitest": "^4.0.12"
  }
}
